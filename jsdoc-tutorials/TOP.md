The , which represents a _tree_ or a _subtree_ node inside a tree) representing a hierarchical filter expression. For example, a simple tree with one "leaf"

```
<FilterTree>
    |
<FilterLeaf>
  /  |  \
"A" "=" "2"
```
